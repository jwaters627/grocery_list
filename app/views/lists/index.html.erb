<h1>Lists</h1>

<div class="lists index">
		<h2>Lists</h2>

		<%= link_to("Add New List", new_list_path, :class => 'action new') %>

		<table class="listing" summary "Lists list">
			<tr class="header">
				<th>#</th>
				<th>List</th>
				<th>Actions</th>
			</tr>

			<% @lists.each do |list| %>
			<tr>
				<td><%= list.position%></td>
				<td><%= list.name%></td>
				<td>
					<%= link_to("Show", list_path(list), :class => 'action show') %>
					<%= link_to("X", '#', :class => 'action delete') %>
			</tr>
			<% end %>
		</table>
	</div>